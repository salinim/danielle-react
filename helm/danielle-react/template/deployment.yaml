apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "danielle-react.fullname" . }}
  labels:
    app: {{ include "danielle-react.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "danielle-react.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "danielle-react.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
