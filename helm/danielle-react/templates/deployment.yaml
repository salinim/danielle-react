apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "danielle-react.fullname" . }}
  labels:
    app: {{ include "danielle-react.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "danielle-react.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "danielle-react.name" . }}
    spec:
      containers:
        - name: {{ include "danielle-react.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80